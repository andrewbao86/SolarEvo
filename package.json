{
  "name": "bess-calculator",
  "version": "1.0.0",
  "description": "BESS Capacity Calculator",
  "scripts": {
    "test": "echo 'No tests specified'",
    "start": "serve .",
    "prebuild": "mkdir -p dist",
    "build": "cp *.html *.js *.css *.svg dist/ || true",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
} 